# Copy to uoe.env and fill in the real values.
# cp uoe.env.example uoe.env && chmod 600 uoe.env
# Required: the script won't run without these (it's the upload destination).

UOE_URL=https://uoe.example.com/v1/upload:/
UOE_TOKEN=change_me
