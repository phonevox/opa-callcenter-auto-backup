# Copy to discord.env and fill in the real value.
# cp discord.env.example discord.env && chmod 600 discord.env
# Optional: if left empty, the script just skips Discord notifications.

DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxxx/yyyy
